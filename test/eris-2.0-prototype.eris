 class Point {	
	def constructor(x, y) {
		this.x = x;
		this.y = y;
	}

    def calc() {
        return this.x + this.y;
    }
}
 


let points = List();

for(let i = 0; i < 5; i += 1) {
    points.add(Point(i, i));
}

points.each(\(elem) {
    println(elem.calc());
});