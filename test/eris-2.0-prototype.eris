 class Point {	
	def constructor(x, y) {
		this.x = x;
		this.y = y;
	}

    def toString() {
        return "(" + this.x.toString() + ", " + this.y.toString() + ")";
    }
}

let points = List();

for(let i = 0; i < 5; i += 1) {
    points.add(Point(Math.random, Math.random));
}

points.each(println)