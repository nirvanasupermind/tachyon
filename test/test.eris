// global scope
// -> factorial function scope
// -> first pair of braces scope
// -> second pair of braces scope
// -> back to global scope (to print the result)

def f(n) {
    if(n == 0) {
        return 1;
    } else {
        return n * f(n - 1);
    }
}

print(f(5));