// Generated by Eris
#include "../src/eris.h"
int main(int argc, char** argv) {
    ErisVal one = ErisVal::make_num(1);
    ErisVal hundred = ErisVal::make_num(100);
    ErisVal tenthousand = ErisVal::make_num(10000);
    ErisVal zero = ErisVal::make_num(0);

    ErisVal t1 = System.o->get("time")({ System });
    ErisVal fac = ErisVal::make_func([&](const std::vector<ErisVal>& args) {
        ErisVal x = args.at(0);
        {
            if (((x == zero)).b) {
                return one;
            }
            else {
                return (x * fac({ (x - one) }));
            }
        }
        return ErisVal::make_nil();
        });
    ErisVal i = zero;
    while (((i < tenthousand)).b) {
        fac({hundred});
        (i = (i + one));
    }
    ErisVal t2 = System.o->get("time")({ System });
    System.o->get("print")({ System,(t2 - t1) });
    System.o->get("print")({ System,i });
    return 0;
}